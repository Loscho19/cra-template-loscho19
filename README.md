# cra-template-loscho19

Use Template:
`yarn create react-app app-name --template cra-template-loscho19`

After created the new App install this devDependencies:
`yarn add prettier redux-devtools-extension -D`
